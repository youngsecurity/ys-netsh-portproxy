# ys-netsh-portproxy

A NETSH Port Proxy GUI for Microsoft Windows.

## Repository layout

- `apps/ys-PortProxyGUI-master` — Young Security snapshot of `zmjack/PortProxyGUI`.
- `apps/ys-PortProxyGooey-master` — Young Security snapshot of `jscottelblein/PortProxyGooey`.
- `src/` — planned new implementation.
- `docs/upstream-sync-and-language-decision.md` — upstream comparison, selective sync record, and rewrite stack decision.
